name: repo_DEP-1_RTD-1-test
channels: 
  - conda-forge 
  - defaults
dependencies:
    # Base depends
  - python
  - pip

    # MDAnalysis
  - cython
  - numpy
  - MDAnalysis

    # Testing
  - pytest
  - pytest-cov
  - pytest-xdist
  - codecov

    # Pip-only installs
  #- pip:
  #  - codecov

